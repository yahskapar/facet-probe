# Data Directory

This repository does not store upstream dataset content.

Use this directory for local, ignored downloads or generated manifests when
running experiments from a checkout. Public release artifacts live in
`../artifacts/`.
