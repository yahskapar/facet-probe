# Optional closed-source provider credentials.
# Fill these locally; never commit real keys.
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional OpenAI-compatible local/hosted endpoint, such as vLLM.
FACET_PROBE_OPENAI_COMPAT_BASE_URL=
FACET_PROBE_OPENAI_COMPAT_API_KEY=

# Optional HuggingFace settings for gated datasets/models and local cache control.
HF_TOKEN=
HF_HOME=
